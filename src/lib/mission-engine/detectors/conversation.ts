import type { Mission } from '../types'
import type { TeacherSituation } from '../../teacher-brain/types'

/**
 * Détecte les missions issues de l'historique de conversation Préparer.
 * ME-02 : architecture uniquement — logique métier à implémenter dans un sprint futur.
 */
export async function detectConversation(_situation: TeacherSituation): Promise<Mission[]> {
  return []
}
