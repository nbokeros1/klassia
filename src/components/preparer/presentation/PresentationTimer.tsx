'use client'

// SCAFFOLD — Minuteur en mode présentation. SC-02E Sprint 3.

interface PresentationTimerProps {
  targetMinutes: number | null
  onToggle:      () => void
}

export function PresentationTimer(_props: PresentationTimerProps) {
  return null
}
