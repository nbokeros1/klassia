'use client'

// SCAFFOLD — Barre de navigation en mode présentation. SC-02E Sprint 3.

interface PresentationNavProps {
  currentIndex: number
  total:        number
  onPrev:       () => void
  onNext:       () => void
}

export function PresentationNav(_props: PresentationNavProps) {
  return null
}
