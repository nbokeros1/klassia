'use client'

// SCAFFOLD — Un slide en mode présentation. SC-02E Sprint 3.

import type { BlocPedagogique } from '@/lib/types/workspace'

interface PresentationSlideProps {
  bloc:       BlocPedagogique
  showNotes:  boolean
}

export function PresentationSlide(_props: PresentationSlideProps) {
  return null
}
