'use client'

// SCAFFOLD — Overlay de génération en cours (splash 15-30s). SC-02E.

interface GenerationStatusProps {
  progress: number   // 0–100
  label?:   string
}

export function GenerationStatus(_props: GenerationStatusProps) {
  return null
}
